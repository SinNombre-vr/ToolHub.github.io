# Catálogo compartido de Assets

`assets.json` es el catálogo público que lee ToolHub desde GitHub Pages.

No guardes contraseñas, tokens de GitHub ni datos privados dentro de esta carpeta.
El Cloudflare Worker incluido en `cloudflare-worker/` actualiza este archivo mediante la GitHub Contents API.
